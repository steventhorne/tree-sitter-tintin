; Scopes
; ------

(command) @scope
(block) @scope

; Definitions
; -----------

variable_declaration: (command name: (word) @definition.var)

; References
; ----------

(variable_reference) @reference.var
(variable_reference) @reference.var
